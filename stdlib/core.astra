module std.core

# Core type aliases and utilities
type Unit = ()

# Identity function
fn identity[T](x: T) -> T { x }

# Constant function (always returns first argument)
fn constant[T, U](x: T, unused: U) -> T { x }
