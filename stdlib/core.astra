module std.core

type Unit = Unit

## Returns the value unchanged. Useful for higher-order functions.
public fn identity[T](x: T) -> T
{
  x
}

## Always returns the first argument, ignoring the second.
public fn constant[T, U](x: T, _unused: U) -> T
{
  x
}
